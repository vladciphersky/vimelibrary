```js
// Получить информацию о токене игрока
// Аргументы: token

// token - токен игрока

// Существует 3 вида токенов: DEV, AUTH, LEGACY
// DEV - токен разработчика, у него есть лимит, но нет возможности узнать его владеьца
// AUTH - токен для авторизации, используется для авторизации игрока на вашем сайте или в любом другом месте, его время действия 1 час
// LEGACY (старый тип токена) - DEV и AUTH вместе, его можно было получить до 04.04.2018

misc.token('xxx').then((data) => {
    console.log(data);
    /*
    {
        "token": "xxx",
        "valid": true,
        "type": "DEV",
        "limit": 300
    }
    */
});
```